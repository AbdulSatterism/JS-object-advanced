// const myNumber = -5;
// const output = Math.abs(myNumber); //abs avoid possitive and negative 

const number = 10.25;
// const output = Math.ceil(number)
// const output = Math.floor(number)
// const output = Math.round(number)


// console.log(output)

// calculate random number
/* 
const randomNumber = Math.random() * 10000;
const randomRound = Math.round(randomNumber)
console.log(randomRound)
*/

// random number with loop

for (let i = 0; i <= 10; i++) {
    const randomNumber = Math.random() * 10000;
    const randomRound = Math.round(randomNumber)
    console.log(randomRound)
}
